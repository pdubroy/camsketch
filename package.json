{
  "private": true,
  "devDependencies": {
    "eslint": "^2.7.0",
    "eslint-config-google": "^0.4.0"
  },
  "scripts": {
    "build-installer": "bash bin/build-installer",
    "prepublish": "npm run lint && npm run build-installer",
    "lint": "eslint **/*.js"
  }
}
